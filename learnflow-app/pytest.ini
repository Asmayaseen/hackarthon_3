[pytest]
markers =
    integration: marks tests that require a running stack (deselect with -m "not integration")
testpaths =
    services/triage-service/tests
    services/progress-service/tests
    tests
asyncio_mode = auto
